{
  "name": "nodebox",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && BUILD_TARGET=gh-pages vite build",
    "dev:server": "tsx watch src/server.ts",
    "build:server": "tsc src/server.ts",
    "start:server": "node src/server.js"
  },
  "devDependencies": {

    "typescript": "^4.5.4",
    "vite": "^3.0.7",
    "tsx": "^4.0.1"
  }
}